id: transforms.pick_object
version: 0.1.0
entrypoint: blocks/processing/transforms/pick_value.py:PickObjectBlock
inputs:
  source:
    type: any
  path:
    type: string
outputs:
  value:
    type: any
requirements: []
description: |
  入力オブジェクトから任意の値をパス指定で抽出する補助ブロック。

id: transforms.pick_object
version: 0.1.0
entrypoint: blocks/processing/transforms/pick_value.py:PickObjectBlock
inputs:
  source:
    type: object
  path:
    type: string
outputs:
  value:
    type: object
description: sourceオブジェクトからドットパスで値を抽出する
